import React, { useContext } from 'react'

const CartScreen = () => {
    cost {state, dispatch: ctxDispatch} = useContext(Store)
  return (
    <div>
      
    </div>
  )
}

export default CartScreen
